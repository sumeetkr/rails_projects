<%= form_for(@scanner) do |f| %>
  <% if @scanner.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@scanner.errors.count, "error") %> prohibited this scanner from being saved:</h2>

      <ul>
      <% @scanner.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :identifier %><br />
    <%= f.text_field :identifier %>
  </div>
  <div class="field">
    <%= f.label :beconId %><br />
    <%= f.number_field :beconId %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
